# このシステムが使うPythonのパッケージ。
# ここに書いておくと Dependabot が新しい版を見つけて
# 自動でプルリクエストを作ってくれる（.github/dependabot.yml）。
#
# 版を固定しすぎない理由：
#   固定すると自動更新の恩恵が受けられず、緩すぎると突然壊れる。
#   メジャー版だけを縛り、その中では新しいものを使う。
#   （実例：pandas 3.0 で欠損値の扱いが変わり、空欄判定が壊れたことがある）
pandas>=3.0,<4
openpyxl>=3.1,<4
